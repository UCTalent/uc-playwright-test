steps:
  # ---------------------------------------------------------------------------
  # Step 1: Validate required files exist in source before deploying (if source is present)
  # ---------------------------------------------------------------------------
  - name: 'bash'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        if [ -f "package.json" ]; then
          echo "=== Pre-flight: Validating source ==="
          [ -f "package-lock.json" ]    || { echo "ERROR: package-lock.json is missing!"; exit 1; }
          echo "✓ package-lock.json"
          [ -f "playwright.config.ts" ] || { echo "ERROR: playwright.config.ts is missing!"; exit 1; }
          echo "✓ playwright.config.ts"
          [ -f "scripts/run-nightly-tests.sh" ] || { echo "ERROR: scripts/run-nightly-tests.sh is missing!"; exit 1; }
          echo "✓ run-nightly-tests.sh"
          echo "=== Source OK ==="
        else
          echo "=== Running without source (--no-source flag active or empty workspace) ==="
        fi

  # ---------------------------------------------------------------------------
  # Step 2: Deploy source code (if present) and run E2E immediately
  # ---------------------------------------------------------------------------
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk:alpine'
    entrypoint: 'sh'
    args:
      - '-c'
      - |
        set -e
        apk add --no-cache openssh-client rsync

        # --- Setup SSH ---
        mkdir -p /root/.ssh
        chmod 700 /root/.ssh
        printenv VM_SSH_KEY | sed 's/\\n/\n/g' | tr -d '\r' > /root/.ssh/id_rsa
        chmod 600 /root/.ssh/id_rsa
        if ! ssh-keygen -y -f /root/.ssh/id_rsa > /dev/null; then
          echo "ERROR: Invalid SSH key in Secret Manager."
          exit 1
        fi

        # --- Check if local source code exists ---
        IF_DEPLOY="false"
        if [ -f "package.json" ] && [ -f "playwright.config.ts" ]; then
          IF_DEPLOY="true"
          echo "=== Local source code found. Deploying before run... ==="
        else
          echo "=== No local source code found. Skipping deploy/rsync step. ==="
        fi

        if [ "$$IF_DEPLOY" = "true" ]; then
          echo "=== Creating directories on VPS ==="
          ssh -i /root/.ssh/id_rsa -o IdentitiesOnly=yes -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o Port="$$SSH_PORT" "$_VM_USERNAME@$_VM_HOST" "sudo mkdir -p '$_E2E_DEPLOY_DIR' '$_E2E_LOG_DIR' '$_E2E_REPORT_DIR' && sudo chown -R '$_VM_USERNAME:$_VM_USERNAME' '$_E2E_DEPLOY_DIR' '$_E2E_LOG_DIR' '$_E2E_REPORT_DIR'"

          echo "=== Ensuring rsync is installed on VPS ==="
          ssh -i /root/.ssh/id_rsa -o IdentitiesOnly=yes -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o Port="$$SSH_PORT" "$_VM_USERNAME@$_VM_HOST" "which rsync >/dev/null 2>&1 || { sudo apt-get update && sudo apt-get install -y rsync; }"

          echo "=== Generating local .env template ==="
          printf "%s\n" \
            "# Playwright E2E — generated by Cloud Build (do not commit)" \
            "CI=true" \
            "CI_AUTH_MODE=reuse" \
            "FRONTEND_URL=$_E2E_FRONTEND_URL" \
            "BASE_URL=$_E2E_FRONTEND_URL" \
            "ATS_URL=$_E2E_ATS_URL" \
            "API_BASE_URL=$_E2E_API_URL" \
            "E2E_LOG_DIR=$_E2E_LOG_DIR" \
            "E2E_REPORT_DIR=$_E2E_REPORT_DIR" > .env.template

          echo "=== Syncing source code to VPS ==="
          rsync -az --delete -s \
            --exclude='.git/' \
            --exclude='.agents/' \
            --exclude='node_modules/' \
            --exclude='playwright-report/' \
            --exclude='nightly-logs/' \
            --exclude='nightly-reports/' \
            --exclude='storageState.json' \
            --exclude='.env' \
            -e "ssh -i /root/.ssh/id_rsa -o IdentitiesOnly=yes -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o Port=\"$$SSH_PORT\"" \
            ./ "$_VM_USERNAME@$_VM_HOST:$_E2E_DEPLOY_DIR/"

          rm -f .env.template

          echo "=== Installing dependencies on VPS ==="
          ssh -i /root/.ssh/id_rsa -o IdentitiesOnly=yes -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o Port="$$SSH_PORT" "$_VM_USERNAME@$_VM_HOST" "bash '$_E2E_DEPLOY_DIR/scripts/setup-vps-deps.sh' '$_E2E_DEPLOY_DIR'"

          echo "=== Writing .env on VPS ==="
          ssh -i /root/.ssh/id_rsa -o IdentitiesOnly=yes -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o Port="$$SSH_PORT" "$_VM_USERNAME@$_VM_HOST" "bash '$_E2E_DEPLOY_DIR/scripts/merge-env.sh' '$_E2E_DEPLOY_DIR'"
        fi

        echo "=== Debug: Checking paths on VPS ==="
        ssh \
          -i /root/.ssh/id_rsa \
          -o IdentitiesOnly=yes \
          -o StrictHostKeyChecking=no \
          -o UserKnownHostsFile=/dev/null \
          -o Port="$$SSH_PORT" \
          "$_VM_USERNAME@$_VM_HOST" \
          "ls -la /opt/ /opt/uctalents/ /opt/uctalents/playwright-tests/ /opt/uctalents-e2e/ 2>&1 || true"

        echo "=== Triggering E2E test run on VPS ==="
        ssh \
          -i /root/.ssh/id_rsa \
          -o IdentitiesOnly=yes \
          -o StrictHostKeyChecking=no \
          -o UserKnownHostsFile=/dev/null \
          -o Port="$$SSH_PORT" \
          "$_VM_USERNAME@$_VM_HOST" \
          "ENV_PREFIX=dev E2E_DIR='$_E2E_DEPLOY_DIR' E2E_LOG_DIR='$_E2E_LOG_DIR' E2E_REPORT_DIR='$_E2E_REPORT_DIR' bash '$_E2E_DEPLOY_DIR/scripts/run-nightly-tests.sh'"

        echo ""
        echo "========================================================"
        echo "✅ Trigger run complete!"
        echo "   Logs  : $_E2E_LOG_DIR"
        echo "   Report: $_E2E_REPORT_DIR"
        echo "========================================================"
    secretEnv: ['VM_SSH_KEY', 'SSH_PORT']

availableSecrets:
  secretManager:
  - versionName: projects/$PROJECT_ID/secrets/dev-vm-ssh-key/versions/latest
    env: 'VM_SSH_KEY'
  - versionName: projects/$PROJECT_ID/secrets/ssh-port-dev/versions/latest
    env: 'SSH_PORT'

substitutions:
  _VM_HOST: '14.225.210.119'
  _VM_USERNAME: 'alphaforce'

  # --- E2E deploy paths on VPS ---
  _E2E_DEPLOY_DIR: '/opt/uctalents/playwright-tests'
  _E2E_LOG_DIR:    '/opt/uctalents/playwright-logs'
  _E2E_REPORT_DIR: '/opt/uctalents/playwright-reports'

  # --- Target URLs (used for .env generation) ---
  _E2E_FRONTEND_URL: 'https://uctalent.dev'
  _E2E_ATS_URL: 'https://business.uctalent.dev'
  _E2E_API_URL: 'https://api.nest.uctalent.dev'

options:
  substitutionOption: ALLOW_LOOSE
  logging: CLOUD_LOGGING_ONLY
